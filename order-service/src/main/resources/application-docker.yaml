eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka-service:8761/eureka/
server:
  error:
    include-exception: false
spring:
  mvc:
    problemdetails:
      enabled: false
  datasource:
    url: jdbc:postgresql://db-orders:5432/order

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
